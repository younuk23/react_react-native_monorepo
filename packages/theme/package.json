{
  "name": "@mono/theme",
  "version": "1.0.0",
  "main": "./dist/index.js",
  "typings": "./dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "rm -rf ./dist && tsc -p tsconfig.json",
    "prewatch": "rm -rf ./dist",
    "watch": "tsc -p tsconfig.json --watch"
  },
  "license": "MIT",
  "devDependencies": {
    "typescript": "^4.0.3"
  }
}
